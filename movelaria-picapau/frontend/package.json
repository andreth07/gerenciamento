{
  "name": "movelaria-picapau-frontend",
  "version": "1.0.0",
  "description": "Frontend do sistema de RH da Movelaria Pica Pau",
  "scripts": {
    "start": "live-server src --port=5500",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}